{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "(npm --prefix room install --no-audit --no-fund || true) && npm --prefix web install --no-audit --no-fund && npm --prefix web run build"
  },
  "deploy": {
    "startCommand": "npm --prefix web run start",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
