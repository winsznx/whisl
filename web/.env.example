# This instance's own participant wallet (BIP39 seed). Lives on this machine only, never in git,
# never sent to the browser. There is no shared server wallet — one instance per participant.
WHISL_WALLET_SEED=

# Public base URL of this instance (used for OG image absolute URLs).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
