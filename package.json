{
  "name": "whisl",
  "private": true,
  "description": "Whisl monorepo — deploy target is web/ (Next.js). room/ loads at runtime.",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "setup": "npm --prefix contracts install && npm --prefix room install && npm --prefix app install && npm --prefix web install",
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix room install && npm --prefix web install && npm --prefix web run build",
    "start": "npm --prefix web run start",
    "test:room": "npm --prefix room test",
    "test:web": "npm --prefix web test"
  }
}
